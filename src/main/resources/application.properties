# ==============================================================
# ===============  SUPABASE DATABASE CONNECTION  ===============
# ==============================================================

spring.datasource.url=jdbc:postgresql://dpg-d404c7f5r7bs73a5opdg-a:5432/cooliedb
spring.datasource.username=cooliedb_user
spring.datasource.password=kFmG1sYdykhXHPIL4qdpGLuSIu4RRQfO
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================================================
# ===============  JPA / HIBERNATE CONFIGURATION  ===============
# ==============================================================

# Automatically create/update tables based on your entities
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in the console (optional for debugging)
spring.jpa.show-sql=true

# Use PostgreSQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Pretty-print SQL output (optional)
spring.jpa.properties.hibernate.format_sql=true

# ==============================================================
# ===============  SERVER / GENERAL SETTINGS  ==================
# ==============================================================

# Change port if needed (default 8080)
server.port=8080

# Optional: Allow requests from React frontend during dev
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
